dependencies {
    compile project(':fipe-service-core')

    compile 'io.micronaut:micronaut-http:1.1.2'
}

publishing {
    publications {
        contract(MavenPublication) {
            from components.kotlin
        }
    }
}