micronaut:
  application:
    name: fipe-service
  config-client:
    enabled: true
  server:
    port: 8080
    cors:
      enabled: true

gateways:
  fipe:
    endpoint: http://fipeapi.appspot.com/api/1

#consul:
#  client:
#    registration:
#      enabled: true
#    defaultZone: "${CONSUL_HOST:localhost}:${CONSUL_PORT:8500}"
